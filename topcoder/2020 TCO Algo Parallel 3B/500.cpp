#include <map>
#include <queue>
#include <cmath>
#include <ctime>
#include <vector>
#include <cstdio>
#include <complex>
#include <cstdlib>
#include <cstring>
#include <iostream>
#include <algorithm>
#include <tr1/unordered_map>
#define num(x) (x>='0' && x<='9')
typedef unsigned long long ull;
typedef long long ll;
using namespace std::tr1;
using namespace std;
class ShortBugPaths{
	int count(int n, vector<int> d, int m){
		int ma=0, len=d.size();
		for (int i=0;i<len;i++) ma=max(ma, d[i]);
		if (ma*m)
	}
};
int main(){
	return 0;
}
